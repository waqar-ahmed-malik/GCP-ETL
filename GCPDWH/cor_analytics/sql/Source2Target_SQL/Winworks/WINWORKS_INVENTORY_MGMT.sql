SELECT
  PARSE_DATETIME('%d-%h-%y %H.%M.%S.000000000 %p',
    W_DATE) AS W_DATE,
  PART_NUMBER,
  ORDER_NUMBER,
  CAST(COST AS FLOAT64)COST,
  CAST(PRICE AS FLOAT64)PRICE,
  CAST(QUAN AS FLOAT64)QUAN,
  CAST(SPECIAL_ORDER AS INT64)SPECIAL_ORDER,
  SUPP_ID,
  DESC_FLD,
  REF,
  CAST(CASH_PAY AS INT64)CASH_PAY,
  EMPL_ID,
  CAST (DEPT_ID AS INT64)DEPT_ID,
  CAST (PO_NUM AS INT64)PO_NUM,
  WO_REF,
  PARSE_DATETIME('%d-%h-%y %H.%M.%S.000000000 %p',
    REC_DATE) AS REC_DATE,
  PARSE_DATETIME('%d-%h-%y %H.%M.%S.000000000 %p',
    CREATE_DTTIME) AS CREATE_DTTIME,
  CREATE_BY,
  SOURCE_SYSTEM_CD
FROM
  `aaadata-181822.COR_WINWORKS.STG_INVENTORY_MGMT`